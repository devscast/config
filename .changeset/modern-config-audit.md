---
"@devscast/config": major
---

Release the breaking v2 async-only configuration API with schema-typed environment properties, explicit JSON/YAML/inline/provider sources, immutable results, structured diagnostics, provenance, isolated dotenv parsing, and no legacy accessor, command expansion, or INI behavior.
